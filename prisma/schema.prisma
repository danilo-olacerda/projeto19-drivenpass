datasource db {
  url      = env("DATABASE_URL")
  provider = "postgresql"
}

model users {
  id       Int       @id @default(autoincrement())
  email String
  password String
  @@unique([email])
}

generator client {
  provider = "prisma-client-js"
}